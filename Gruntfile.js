// Generated by CoffeeScript 1.9.3
(function() {
  module.exports = function(grunt) {
    var concurrentConfig, karmaConfig, karmaServersCount, karmaStartPort, karmaTestConfig, myConf, ref, ref1;
    require('load-grunt-tasks')(grunt);
    require('time-grunt')(grunt);
    myConf = {
      build: 'build',
      version: '1.8.0',
      chromePath: 'C:/Program Files (x86)/Google/Chrome/Application/chrome.exe',
      karmaChromeUsersDir: 'c:/www/karmaTestChromeUserDir'
    };
    karmaTestConfig = grunt.file.read('test/config.coffee');
    karmaStartPort = parseInt((ref = karmaTestConfig.match(/karmaStartPort[ ]*=[ ]*([0-9]{4,5})/)) != null ? ref[1] : void 0);
    karmaServersCount = parseInt((ref1 = karmaTestConfig.match(/okServersCount[ ]*=[ ]*([0-9]{1,5})/)) != null ? ref1[1] : void 0);
    console.log("karmaStartPort = " + karmaStartPort + ", karmaServersCount = " + karmaServersCount);
    concurrentConfig = {
      test: {
        tasks: [],
        options: {
          limit: 50,
          logConcurrentOutput: false
        }
      }
    };
    karmaConfig = {
      options: {
        autoWatch: false,
        configFile: 'karma.conf.js',
        singleRun: true,
        customLaunchers: {}
      }
    };
    grunt.initConfig({
      myConf: myConf,
      concat: {
        build: {
          options: {
            banner: "/*\n * Oktell.js\n * version <%= myConf.version %>\n * http://js.oktell.ru/\n */\n\n"
          },
          files: [
            {
              src: ['oktell.js'],
              dest: '<%= myConf.build %>/oktell.js'
            }
          ]
        }
      },
      uglify: {
        build: {
          files: {
            '<%= myConf.build %>/oktell.min.js': ['<%= myConf.build %>/oktell.js']
          }
        }
      },
      replace: {
        build: {
          src: ['oktell.*'],
          overwrite: true,
          replacements: [
            {
              from: /self.version = '[0-9\.]+'/g,
              to: "self.version = '<%= myConf.version %>'"
            }
          ]
        },
        bower: {
          src: ['bower.json'],
          overwrite: true,
          replacements: [
            {
              from: /"version": "[0-9\.]+",/,
              to: '"version": "<%= myConf.version %>",'
            }
          ]
        }
      },
      karma: karmaConfig,
      concurrent: concurrentConfig,
      parallel: {
        assets: {
          options: {
            grunt: true
          },
          tasks: ['karma:first', 'karma:second']
        }
      },
      connect: {
        server: {
          options: {
            hostname: '127.0.0.1',
            port: 6574,
            keepalive: true,
            open: true
          }
        }
      }
    });
    grunt.registerTask('build', ['replace', 'concat:build', 'uglify:build']);
    return grunt.registerTask('default', ['build']);
  };

}).call(this);
